syntax = "proto3";

package stream;
import "simple.proto";

// CSStream definition
service CSStream {
    rpc Stream(stream Package.Frame) returns (stream Package.Frame);
}

/// SimpleService definition
service SimpleService {
    rpc SimpleRequest(Package.Frame) returns (Package.Frame);
}
